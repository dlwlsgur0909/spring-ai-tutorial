spring:
  application:
    name: spring-ai-tutorial
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_ai_tutorial
    username: postgres
    password: admin1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
  ai:
    model:
      chat: ollama
      embedding: ollama
    ollama:
      base-url: http://localhost:11434 # ollama default url
      init:
        pull-model-strategy: never # 초기화 전략 (모델이 없는 경우 pull 여부)
        timeout: 1m # 모델 pull 또는 초기화 대기 시간
        max-retries: 0 # model 초기화 실패 시 재시도 횟수
        chat:
          include: true # 초기화 전략에 포함 여부
        embedding:
          include: true # 초기화 전략에 포함 여부
      chat:
        model: gemma4 # gemma3는 tool calling을 지원하지 않아서 모델 변경
#        format: json
        keep_alive: 5m
        memory:
          repository:
            jdbc:
              initialize-schema: never
      embedding:
        model: nomic-embed-text
        keep_alive: 5m # 임베딩이 끝난 후 모델을 메모리에 유지할 시간으로 기본값은 5m

#  ai:
#    openai:
#      api-key: open-ai-test-key
#    huggingface:
#      chat:
#        api-key: ${HUGGINGFACE_API_KEY}
#        url: https://api-inference.huggingface.co/models/Qwen/Qwen2.5-7B-Instruct