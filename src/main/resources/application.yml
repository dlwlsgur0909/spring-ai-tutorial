spring:
  application:
    name: spring-ai-tutorial
  ai:
    openai:
      api:
        key: open-ai-test-key
    huggingface:
      api-key: ${HUGGINGFACE_API_KEY}
      chat:
        options:
          model: mistralai/Mistral-7B-Instruct